paths:
  raw_dir: data/raw
  normalized_dir: data/normalized
  merged_dir: data/merged
  signals_dir: data/signals

books:
  sharp_reference: ["Draftkings"]   
  active_books: ["Fanduel", "Draftkings", "betMGM", "ProphetX"]

signals:
  bankroll: 1000.0
  min_ev: 0.01            
  kelly_mult: 0.25        
  max_frac_per_bet: 0.02 
  max_age_ms: 30000      
